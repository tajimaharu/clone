<div class="container-fluid">
  <div class="row">
    <div class="left-bar col-md-2">
      <h3>twitterクローン</h3>
      <h4>ページサイズで要素非表示</h4>
    </div>
    <div class="content col-md-7">
      <%= form_with(model: @clone, local: true) do |form| %>

      <%= form.label :content %>
      <br>
      <%= form.text_area :content %>
      </br>
      <%= form.submit value="Tweet"%>
      <% end %>

      <br>
      <h2>tweet</h2>
      </br>
      <p><%= notice %></p>

      <% @clones.reverse.each do |clone| %>
        <br>
          <td><%= clone.content %></td>
          <td><%= link_to "削除", clone_path(clone.id), method: :delete, data: { confirm: '本当に削除していいですか？' }, class: "btn" %></td>
          <td><%= link_to "編集", edit_clone_path(clone.id), class: "btn" %></td>
        </br>
      <% end %>

    </div>
    <div class="right-bar col-md-3">
      <h3>広告とかトレンドとか表示されてる場所</h3>
    </div>
  </div>
</div>
